{% extends 'base.html.twig' %}

{% block title %}Création d'une annonce{% endblock %}

{% form_theme form 'ad/_collection.html.twig' %}

{% block body %}

    {# {{ form(form) }} #}
    {# {{ form_row(form.title)}} #}
    {# {{ form_label(form.title)}} #}
    
    <div class="container">
        <h1 class="my-5">Créer une nouvelle annonce</h1>

        {{ form_start(form) }}
        
        <div class="row">
            <div class="col">
                <div class="alert alert-light">
                    <h2 class="alert-heading">Information générales</h2>
                    <hr>
                    {{ form_row(form.title) }}
                    {{ form_row(form.slug) }}
                    {{ form_row(form.price) }}
                    {{ form_row(form.rooms) }}
                </div>
                <div class="alert alert-light">
                    <h2 class="alert-heading">Détail de l'annonce</h2>
                    <hr>
                    {{ form_row(form.introduction) }}
                    {{ form_row(form.content) }}
                </div>
            </div>
            <div class="col">
                <div class="alert alert-light">
                    <h2 class="alert-heading">Images de l'annonce</h2>
                    <hr>
                    {{ form_row(form.coverImage) }}
                    {{ form_row(form.images) }}
                </div>
            </div>
        </div>
        
        {# {{ form_widget(form) }} #}

        {# <div class="form-group">
            {{ form_label(form.title) }}
            {{ form_widget(form.title, {'attr' : {'class' : 'form-control', 'placeholder' : 'Titre de l\'annonce'} }) }}
        </div>
        <div class="form-group">
            {{ form_label(form.introduction) }}
            {{ form_widget(form.introduction, {'attr' : {'class' : 'form-control', 'placeholder' : 'Titre de l\'annonce'} }) }}
        </div> #}

        <div class="alert alert-success clearfix">
            <h2 class="alert-heading">Sauvegarder mon annonce</h2>
            <p>Vous êtes sur le point de créer une annonce, félicitation !</p>
            <button type="submit" class="btn btn-primary float-right"><i class="fas fa-check"></i> Créer la nouvelle annonce</button>
        </div>
        {{ form_end(form) }}

    </div>

{% endblock %}

{% block javascripts %}
    {# <script src="/js/ad.js"></script> #}
    {{ encore_entry_script_tags('js/ad') }}  
{% endblock %}

{# {% block form_row %} #}
    {# {{ dump() }} #}

    {# <div class="form-group">
        {{ form_label(form) }}
        {{ form_widget(form) }} #}
        {# {{ form_widget(form, {'attr' : {'class' : 'form-control', 'placeholder' : 'test' } }) }} #}
    {# </div>
{% endblock %}

{% block text_widget %}
    <input type="text" name="{{ full_name }}" id="{{ id }}" class="form-control">
{% endblock %} #}
